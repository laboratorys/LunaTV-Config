const valid = []; const ads = []; blocks.forEach((block, i) => { const count = block.length / 2; if (i === 0 || count >= 10) { valid.push(block); } else { if (ads.length > 0) ads.push('#EXT-X-DISCONTINUITY'); block.forEach((line, idx) => { if (line.startsWith('#EXTINF')) { ads.push(line); const ts = block[idx + 1]; if (ts && !ts.startsWith('#')) { ads.push(ts.startsWith('http') ? ts : baseUrl + ts); } } }); } }); return { validBlocks: valid, adSegments: ads };